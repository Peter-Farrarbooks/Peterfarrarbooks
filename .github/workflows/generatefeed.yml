name: Generate RSS Feed

on:
  push:
    branches:
      - main

jobs:
  build-feed:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install beautifulsoup4

      - name: Generate feed.xml
        run: |
          cd peterfarrarbooks
          python generate_feed.py

     - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add peterfarrarbooks/feed.xml peterfarrarbooks/blog/index.html
          git diff --staged --quiet || (git pull --rebase origin main && git commit -m "Auto-update feed.xml and blog index [skip ci]" && git push)
